const name = 'Alejandra';
let hpPoints: number | string = 95; //definir si la variable es de tipo number o string
const isAlive: boolean = true;

hpPoints = 'FULL';

console.log({name, hpPoints, isAlive
})

export {};